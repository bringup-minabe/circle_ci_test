machine:
  timezone: Asia/Tokyo

test:
  override:
    - echo Test

deployment:
  production:
    branch: master
    commands:
      - sh sh/deploy_master.sh

notify:
  webhooks:
    - url: http://shoku.zenbase.jp/ci/hook_json.php