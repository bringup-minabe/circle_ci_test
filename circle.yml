machine:
  timezone: Asia/Tokyo

dependencies:
  pre:
    - sudo pip install awscli

test:
  override:
    - echo Hello World

deployment:
  production:
    branch: master
    commands:
      - sh sh/deploy_master.sh